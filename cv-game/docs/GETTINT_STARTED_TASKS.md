# Weekend 1: The Foundation

- [ ] **Task 1: Project Setup**
    - Create the folder structure.
    - Setup `index.html` and `src/main.js`.
    - Create a `BaseScene` that has a `showMessage(text)` method for dialogue.

- [ ] **Task 2: The Hero's Movement**
    - Implement `src/entities/Player.js`.
    - Add 4-way SNES-style movement (Up, Down, Left, Right).
    - Add an "Action" key (Space/E) to trigger dialogue.

- [ ] **Task 3: The First Room (Saarbrücken)**
    - Load a placeholder tileset.
    - Place 5 NPCs (Parents + 3 Siblings).
    - Link their dialogue to `data/dialogue.json`.

- [ ] **Task 4: The Combat Prototype**
    - Add a "Swing" animation or a simple hitbox in front of Martin when 'Z' or 'Left Click' is pressed.
    - Create a "Bug" entity that disappears when hit.

